# Copy this file to .env and fill in real values. Never commit .env.

# Claude API (Anthropic) - answer generation
ANTHROPIC_API_KEY=

# Voyage AI - query-time embedding only
VOYAGE_API_KEY=

# Neon Postgres (pgvector) - read-only from this repo's perspective;
# the database is populated by the private ingestion pipeline.
NEON_DATABASE_URL=
